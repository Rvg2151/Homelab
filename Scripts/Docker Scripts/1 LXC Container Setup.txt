#Create Docker LXC:

#Run on Host:
bash -c "$(wget -qLO - https://github.com/community-scripts/ProxmoxVE/raw/main/ct/docker.sh)"

#No To Default Options
#Select Advanced
#Select Privliged
#Root PW:
#ID 1xx
#DHCP
#No on Root SSH Access
#Note the IP DHCP Gives
#Yes to Install Portainer
#When Done Change Network to Static IP

#Run on Host:
docker run -d \
--name watchtower \
-v /var/run/docker.sock:/var/run/docker.sock \
containrrr/watchtower

apt install cifs-utils
apt install nfs-common

#Portainer Port to Use 9443 and HTTPS in URL
#https://x.x.x.x:9443


#Don't Forget to set Restart Policy to "Unless Stopped" for Watchtower and Containers you deployed.

