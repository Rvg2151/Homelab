#Run in PVE01 to Create, Run in LXC Container to Update.

bash -c "$(wget -qLO - https://github.com/tteck/Proxmox/raw/main/ct/vaultwarden.sh)"

#Vaultwarden .env file (to find the ADMIN_TOKEN)

cat /opt/vaultwarden/.env
