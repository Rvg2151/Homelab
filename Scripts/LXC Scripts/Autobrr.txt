#To create a new Proxmox VE Autobrr LXC, run the command below in the Proxmox VE Shell.

bash -c "$(curl -fsSL https://raw.githubusercontent.com/community-scripts/ProxmoxVE/main/ct/autobrr.sh)"

#Location of config file
#/root/.config/autobrr/config.toml

